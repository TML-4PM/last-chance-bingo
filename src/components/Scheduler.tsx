// src/components/Scheduler.tsx
import React from 'react';

const Scheduler = () => {
  return (
    <div className="border p-4 rounded">
      <h3 className="font-bold mb-2">Scheduler</h3>
      <p>This is a placeholder for a FullCalendar integration with drag‑and‑drop scheduling and real‑time availability.</p>
      {/* In production, integrate FullCalendar or a similar library here */}
    </div>
  );
};

export default Scheduler;
